name: Checks
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - run: npm ci
      - run: npm run build
      - run: npx playwright install chromium
      - run: npm run test:e2e
      - run: npm run build:site
        env:
          NEXT_PUBLIC_DOWNLOAD_URL: https://github.com/serkan-uslu/recora-screen/releases/latest/download/Recora-Screen_0.1.0_macOS-arm64.dmg
          NEXT_PUBLIC_BASE_PATH: /recora-screen
      - run: npm run test:native
      - run: npm run prepare:runtime
      - run: npm run desktop:check
